# Recursion Thinking Guide (재귀 함수 사고 과정 정리)

재귀 함수는 처음 배우면 “함수가 자기 자신을 부른다”는 말 때문에 이해하기 어렵다.  
하지만 실제로는 **문제를 더 작은 동일한 문제로 줄여 해결하는 사고 방식**이다.

이 문서는 재귀 문제를 설계할 때 생각해야 할 과정을 정리한 것이다.

---

# 1. 재귀의 핵심 개념

재귀는 다음 구조를 가진다.

큰 문제 = 작은 문제 + 현재 처리

그리고 반드시 **종료 조건(Base Case)** 이 존재해야 한다.

---

# 2. 재귀 함수 설계 과정

재귀 문제를 풀 때는 다음 4단계를 생각한다.

---

## ① 함수의 의미 정의

먼저 이 함수가 **무엇을 반환하는지** 정의한다.

예시

digits(n) → 정수 n의 자릿수 개수  
maxArray(A, n) → 배열 A의 최댓값  
dfs(x, y) → (x, y)에서 갈 수 있는 모든 칸 탐색  

함수 의미가 명확해야 재귀 구조를 설계할 수 있다.

---

## ② 작은 문제 찾기

원래 문제와 **같은 형태지만 더 작은 문제**를 만든다.

조건

- 문제의 형태는 동일해야 한다
- 입력 크기만 줄어야 한다

예시

문제 | 작은 문제
---|---
자릿수 계산 | digits(n // 10)
배열 최댓값 | max(A, n-1)
DFS | dfs(nx, ny)
병합 정렬 | merge_sort(left), merge_sort(right)

---

## ③ 종료 조건 (Base Case)

더 이상 문제를 줄일 수 없는 상태이다.

이 상태에서는 **바로 정답을 반환한다.**

예시

if (n < 10) return 1;

if (n == 1) return A[0];

if (벽이거나 이미 방문한 위치) return;

---

## ④ 현재 단계 처리

작은 문제의 결과를 이용해 **현재 문제의 결과를 만든다.**

예시

자릿수 계산

digits(n) = digits(n / 10) + 1

배열 최댓값

max(A[0..n-1]) = max(max(A[0..n-2]), A[n-1])

병합 정렬

왼쪽 정렬 + 오른쪽 정렬 → merge

---

# 3. 재귀 설계 핵심 원칙

### 1. 작은 문제는 원래 문제와 동일한 형태여야 한다

좋은 예

digits(n) → digits(n / 10)

나쁜 예

digits(n) → digits(n / 2) + digits(n / 3)

---

### 2. 문제는 반드시 작아져야 한다

문제가 줄어들지 않으면 **무한 재귀**가 발생한다.

예

f(n) → f(n)

---

### 3. 종료 조건은 반드시 존재해야 한다

재귀에서 가장 흔한 버그는 **Base Case가 없는 것**이다.

---

# 4. 재귀 실행 흐름

재귀 함수는 다음 두 단계로 진행된다.

1. 재귀 호출 (문제 줄이기)  
2. 반환하면서 결과 계산  

예

digits(1234)

→ digits(123)  
→ digits(12)  
→ digits(1)  
→ return 1  
→ return 2  
→ return 3  
→ return 4  

즉

- 내려갈 때 → 문제를 줄인다  
- 올라올 때 → 결과를 계산한다  

---

# 5. 재귀를 사용하기 좋은 상황

재귀는 다음 문제에서 특히 유용하다.

### 트리 / 그래프 탐색

- DFS
- 연결 요소 찾기
- 미로 탐색

### Divide & Conquer

- Merge Sort
- Quick Sort
- Binary Search

### 모든 경우 탐색

- 백트래킹
- 조합
- 순열

---

# 6. 재귀 vs 반복문

많은 문제는 반복문으로도 해결 가능하다.

재귀를 사용하는 이유

- 문제 구조 표현이 더 직관적
- 트리 / 그래프 탐색에 유리
- 코드가 간결함

---

# 7. 재귀 설계 체크리스트

재귀 문제를 풀 때 스스로 다음 질문을 해보자.

1. 이 함수는 무엇을 반환하는가?  
2. 같은 문제를 더 작은 입력으로 만들 수 있는가?  
3. 언제 종료되는가?  
4. 작은 문제의 결과로 현재 문제를 어떻게 해결하는가?  

---

# 8. 한 줄 요약

재귀는

작은 문제의 결과를 이미 알고 있다고 가정하고  
현재 문제의 나머지 한 부분만 처리하는 방식이다.

result(n) = result(smaller) + current
